#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

int main() {
	char * s = int strSize = strlen("foo") + strlen( "bar") + 1;
char *resultStr = (char *)malloc(strSize);
strcpy(resultStr, "foo");
strcat(resultStr,  "bar");
	printf(s);
	char * s1 = "foo";
	char * s2 = int strSize = strlen("bar") + strlen( s1) + 1;
char *resultStr = (char *)malloc(strSize);
strcpy(resultStr, "bar");
strcat(resultStr,  s1);
	printf(s2);
	printf(int strSize = strlen("foo") + strlen( int strSize = strlen("bar") + strlen( "baz") + 1;
char *resultStr = (char *)malloc(strSize);
strcpy(resultStr) + 1;
char *resultStr = (char *)malloc(strSize);
strcpy(resultStr, "foo");
strcat(resultStr,  int strSize = strlen("bar") + strlen( "baz") + 1;
char *resultStr = (char *)malloc(strSize);
strcpy(resultStr));

	return 0;
}
